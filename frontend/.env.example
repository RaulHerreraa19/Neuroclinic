# Local: URL completa del backend. En producción (Vercel) usar VITE_API_URL=/api:
# vercel.json reenvía /api y /uploads al backend en Render, así la cookie de sesión
# queda en el mismo dominio que el frontend.
VITE_API_URL=http://localhost:4000/api
